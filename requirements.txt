# ── Core ML / NLP ──────────────────────────────────────────────────────────
torch==2.6.0
torchvision==0.21.0
transformers==4.57.3
datasets==4.4.1
accelerate==1.11.0
tokenizers==0.22.1
sentencepiece==0.2.1
protobuf==6.33.0
seqeval==1.2.2
pytorch-crf==0.7.2          # replaces torchcrf==1.1.0; same API (from torchcrf import CRF) but torch-2.x compatible
spacy==3.8.5

# ── Retrieval ───────────────────────────────────────────────────────────────
faiss-cpu==1.12.0       # use faiss-gpu on CUDA machines if preferred
numpy==2.3.3
scipy==1.15.2

# ── Data / utilities ────────────────────────────────────────────────────────
pandas==2.2.3
scikit-learn==1.6.1
networkx==3.5
tqdm==4.67.1

# ── Web layer ───────────────────────────────────────────────────────────────
flask==2.0.1
werkzeug==2.0.3          # flask 2.0.x requires werkzeug<2.1; newer werkzeug removed url_quote
flask-cors==5.0.1
pymongo==4.7.3
marshmallow==3.21.3      # backend/utils/validation.py Schema / fields

# ── Config / environment ────────────────────────────────────────────────────
PyYAML==6.0.1
python-dotenv==1.1.0
requests==2.32.5

# ── Notes ───────────────────────────────────────────────────────────────────
# Install with:  pip install -r requirements.txt
# For CUDA 12.4: pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 \
#                --index-url https://download.pytorch.org/whl/cu124
